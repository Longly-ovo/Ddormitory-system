## 工作原则

1. 优先保证项目可以正常运行。
2. 修改必须直接服务于用户当前明确提出的任务。
3. 优先采用简单、局部、可验证的实现，不进行无关重构。
4. 不删除或破坏已有可运行功能。
5. 不随意引入复杂依赖，不将项目升级为微服务。
6. 除非用户明确要求，不主动加入 Redis、消息队列、Docker、复杂缓存等非必要内容。
7. 遇到报错时优先定位并修复当前问题，不重写整个项目。
8. 如果任务较大，优先分阶段完成。

---

## 执行任务前的检查

开始修改前，确认：

1. 修改是否直接属于当前任务范围。
2. 是否可能破坏已有功能或覆盖用户现有改动。
3. 是否有更简单、影响更小的实现方式。
4. 是否需要同步修改前端、后端、数据库或 README。
5. 是否需要用户手动执行 SQL 或其他操作。
6. 是否应分阶段实施，避免一次性大改。

---

## 修改与验证要求

1. 修改前说明对任务的理解，并列出计划修改的文件。
2. 默认工作区可能存在用户未提交的修改，不得覆盖、回退或格式化无关改动。
3. 每次修改只涉及当前任务所需的文件和逻辑。
4. 修改后说明具体改动、影响范围和测试方法。
5. 执行与改动范围匹配的最小有效验证。
6. 不得为了通过测试而删除测试、绕过校验或掩盖错误。
7. 如果无法完成验证，说明原因、风险以及用户需要执行的操作。
8. 执行可能修改文件、数据库、依赖、Git 状态或外部环境的命令前，说明作用和影响；只读检查无需逐条说明。
9. 每次对项目进行修改或完善后，必须由执行者亲自启动或访问系统完成实际验证，确认修改成功、没有引入新 bug，且原问题已经修复；不能只停留在代码层面说明。

---

## 数据库设计原则

1. 当前数据库以简单可用为主，优先兼容现有表结构和数据。
2. 不改变当前数据库主链路，除非用户明确要求。
3. 修改表结构前说明迁移方案、影响范围和回退方式。
4. SQL 应尽量可重复执行，并避免破坏已有数据。
5. 未经用户明确确认，不得删除表、清空表或批量删除数据。
6. 需要用户手动执行 SQL 时，提供明确的执行步骤和验证方法。

---

## 安全规则

1. 不输出真实密码、Token 或密钥。
2. 不在代码或说明文档中硬编码用户的真实数据库密码。
3. 不删除用户文件，不修改项目目录外的文件。
4. 不访问与当前任务无关的目录或数据。
5. 不执行删除磁盘、清空用户目录等危险命令。
6. 可能影响数据或产生破坏性结果的操作，必须先说明影响并获得用户确认。

---

## 禁止事项

除非用户明确要求，否则禁止：

1. 删除已有功能或重写整个项目。
2. 引入复杂权限系统或当前项目不需要的大型依赖。
3. 把项目变成纯表格管理系统。
4. 一次性修改大量无关文件。
5. 随意删除或批量修改数据库数据。
6. 在未说明影响并获得确认的情况下执行破坏性命令。
